## 新增

### 账号列表
- 统计区域宽度与标题同宽
- 统计行右侧新增**耗时**标签，显示「首字耗时 / 总耗时」（秒），鼠标悬停 tooltip 显示最近 10 次请求平均统计
- 右上角菜单新增「显示耗时统计」开关（默认关闭）
- 统计行点击从「每行独立切换」改为「全局切换」—— 一次点击改变整列的显示模式（统计数字 ↔ 模型列表）

### 统计页面
- 汇总卡片新增**总计 Token**（输入 + 输出 + 缓存读取 + 缓存写入 四桶求和）
- 汇总卡片新增**缓存命中率**（缓存读取 ÷ 提示 Token）
- 窄屏下请求列表从塌缩成方块改为**两行紧凑布局**，宽屏保持七列网格零回归

### 开发体验
- Vite 开发服务器从 `~/.ai-switch/web-service.json` 自动读取 token 并附加到代理请求
- 支持环境变量 `AI_SWITCH_DEV_API_TARGET` 指向非本机 API 服务器（调试远程部署时适用）

## 修复

- **Codex CLI 配 Anthropic 上游完整贯通** —— 修复协议适配缺陷，配置中转站后可通过 Codex 正常访问 Claude 模型
- 补齐 Gemini 桥接的条目类型定义
- 回放的 thinking 签名被上游拒绝时，改写请求体重试（兼容更严格的模型签名校验）

## 其他

- 侧栏设置下新增**关于页面**，包含版本号、官网链接、分享文案与开源致谢
- 完善全应用非线性动效系统（感谢 @HurryBy 的贡献 #6）
- 池内同名模型的上下文窗口取最大值，DeepSeek 等 Harness 适配器写入上下文窗口信息
- 发布 Intel 版 macOS dmg（支持 x86_64 Mac）
- WinGet 包标识符定为 `Lingyun.AISwitch`
