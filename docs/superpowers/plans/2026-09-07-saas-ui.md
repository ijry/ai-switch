# SaaS 用户站点与管理界面实施计划

> 分组部分暂停执行：SaaS 管理页改为核心组的只读列表和倍率/模型映射/价格扩展编辑，删除旧创建/删除/批次成员编辑入口。以 `../specs/2026-09-07-shared-agent-groups-design.md` 为准，设计审阅后修订 Task 3；其他用户页面和设置实现保留。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans task-by-task. Track steps with checkboxes.

**Goal:** 在 React 中复刻 pro-api 用户体验，提供完整真实用户/管理员操作。

**Architecture:** 用户端独立客户端、布局和路由；管理员走现有 Transport；对宿主仅导出入口、设置页、管理页。

**Tech Stack:** React 18、TypeScript、Vitest、现有图标/样式体系。

**Spec:** `docs/superpowers/specs/2026-09-07-saas-plugin-design.md` 第 4、10 节。

## Global Constraints

- 继承总计划契约；只写 `src/saas/`、`tests/saas/`；宿主 App/导航/设置挂载由集成任务负责。
- 参考 `D:\Repos\xyito\open\pro-api\web\user`，不引入 Vue，不使用假统计。
- 美元/人民币显示由后端整数金额决定；CSRF/session 与管理员 token 分离。

## Task 1: 独立客户端与入口

**Files:** `src/saas/api.ts`、`types.ts`、`entry.tsx`、`tests/saas/entry.test.tsx`。

**Interfaces:** 导出 `SaasPortal`、`SaasSettings`、`SaasAdmin`；管理员操作调用 `getTransport().call('saas_admin', { operation, payload })`。用户 fetch 使用 same-origin credentials；GET auth/session 返回 `{user,csrfToken}`，后续 POST 带 `x-saas-csrf`，没有会话不读个人数据。

- [ ] 先写未登录不请求个人信息、Cookie/CSRF、失败状态与根入口渲染测试。

```tsx
expect(screen.getByRole('link', {name: /GitHub/})).toHaveAttribute('href', '/api/saas/auth/github');
expect(screen.queryByText('sk-saas-secret')).not.toBeInTheDocument();
```

- [ ] `pnpm exec vitest run tests/saas` 观察新行为失败。
- [ ] 实现公开配置、用户会话和独立 fetch，不读管理员 token；支持深链接/浏览器返回。
- [ ] 转绿，用户数据请求从选中页面发起而非未登录预取。

## Task 2: 完整用户页面

**Files:** `src/saas/user/`、`src/saas/components/`、`src/saas/saas.css`、`tests/saas/portal.test.tsx`。

- [ ] 测试首页真实金额、Key 创建一次展示、取消待审核充值、兑换成功刷新、表单错误和会话过期。

```tsx
await user.click(screen.getByRole('button', {name: /兑换/}));
expect(await screen.findByText(/到账/)).toBeInTheDocument();
expect(screen.getByRole('navigation')).toBeVisible();
```

- [ ] 测试失败后实现首页/用量/Key/充值/兑换/个人页，接总计划全部用户 operation。
- [ ] 复刻参考卡片和导航交互，样式隔离、移动布局、无数据/错误/加载、键盘和中英文。
- [ ] 转绿，构建不引入 Vue，不把假数据或模拟成功留在页面。

## Task 3: 设置与 SaaS 管理

**Files:** `src/saas/settings/`、`src/saas/admin/`、`tests/saas/admin.test.tsx`。

- [ ] 先测试默认关闭、秘密不回显、端点/批次/模型联动、金额明确的审核、兑换码一次导出。

```tsx
expect(screen.getByRole('checkbox', {name: /启用 SaaS/})).not.toBeChecked();
expect(screen.queryByDisplayValue('actual-client-secret')).not.toBeInTheDocument();
```

- [ ] 失败后实现配置、用户、分组、三类价格/倍率、充值、兑换码、账务核对、日志；全部调用真实管理员 operation。
- [ ] 导出给宿主挂载的组件，不自行改 App/导航/原设置文件。
- [ ] 跑测试与 typecheck，提交相关文件并给出挂载 props/API 类型说明。
